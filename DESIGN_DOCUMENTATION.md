# 📜 وثيقة التصميم المعماري وهندسة الأمان - مشروع Zeno Bot
## (Comprehensive Security and Architecture Design Document)

**تاريخ الإنشاء:** [التاريخ الحالي]
**المساهم الرئيسي:** Hermes Agent AI Assistant
**الهدف من الوثيقة:** توفير سجل مُفصَّل ومُمنهج لجميع التحسينات المعمارية والأمنية التي أُجريت على البوت، وتوثيق أفضل الممارسات الصناعية (Best Practices) لمنع تكرار الثغرات الأمنية في المستقبل.

---

## 🎯 الملخص التنفيذي (Executive Summary)
مرّ مشروع Zeno Bot بمرحلة تدقيق معماري وأمني شاملة (Security and Architecture Audit). تم اكتشاف ثلاث مجموعات من المشاكل الحرجة: **(1) تسريب الأسرار، (2) استخدام API قديم مُهمل، و (3) عدم توحيد نقاط الاتصال بالخدمات الخارجية.**

لمعالجة هذه الثغرات، قمنا بتطبيق نظام هندسي متكامل يتضمن:
*   **تجريد الأسرار:** إنشاء طبقة `SecretManager` لمركزية التحكم في جميع المفاتيح الحساسة (Tokens, Secrets).
*   **التحصين الآمن:** تحديث ثلاث وحدات رئيسية (`index.js`, `dashboard/server.js`, `eventHandler.js`) لتفرض استخدام الـ `SecretManager` بشكل إلزامي، مما يجعل البوت آمنًا من التغييرات في بيئة التشغيل (Runtime Environment) أو عند تغيير مفاتيح API.

---

## 🔎 المرحلة الأولى: التحليل الأمني (Security Audit Findings)
| الثغرة المكتشفة | النطاق المتأثر (Module) | مستوى الخطورة (Severity) | التفسير الهندسي |
| :--- | :--- | :--- | :--- |
| **Secret Leakage in Build Process** | `Dockerfile` & ENV Vars | 🔴 Critical | استخدام الأسرار في متغيرات البناء أو البيئة يسمح باكتشافها من خلال فحص طبقات الصورة (Image Layer Inspection). يجب أن تكون الأسرار متاحة فقط وقت التشغيل. |
| **Direct Secret Usage** | Dashboard Server, Event Handlers | 🔴 High | الوصول المباشر إلى `process.env` يكسر مبدأ فصل الاهتمامات ويجعل الكود عرضة للتعديل غير الآمن. |
| **API Deprecation** | Modules/Events (e.g., Interactions) | 🟠 Medium | الاعتماد على واجهات برمجة تطبيقات قديمة سيؤدي إلى توقف البوت في أي تحديث مستقبلي لمكتبات الطرف الثالث (Discord API). |

---
## 🛡️ المرحلة الثانية: التصحيح المعماري (Architectural Solution Implementation)

تم تطبيق الحل الأمني عبر المكونات التالية:

### 1. وحدة إدارة الأسرار (`src/utils/secretManager.js`)
*   **الوظيفة:** هي قلب نظام الأمن. تعمل كـ Singleton Pattern لضمان أن هناك نقطة وصول واحدة ومراقبة لكل مفتاح سري في البوت.
*   **الميزة الهندسية:** توفر واجهة `getMultipleSecrets()` التي تسمح بالتحقق المجمع من مجموعة أسرار، وتضمن تسجيل تحذير (Fallback Warning) إذا فشل جلب أي سر بشكل آمن، مما يجبر المطور على التعامل مع مشكلة عدم التوفر فوراً.

### 2. تأمين نقطة الدخول الرئيسية (`src/index.js`)
*   **التغيير:** تم تعديل منطق التهيئة (Bootstrapping).
*   **النتيجة الأمنية:** البوت لن يبدأ العمل إطلاقاً إذا لم يتم العثور على التوكنز الأساسية عبر `SecretManager`، مما يمنع "الفشل الصامت" (Silent Failure) عند فقدان مفتاح.

### 3. تأمين واجهة المستخدم (`src/dashboard/server.js`)
*   **التغيير:** تم تحديث جميع مسارات API والتعامل مع الجلسات لـ:
    1.  استخراج `SESSION_SECRET` عبر الـ Secret Manager.
    2.  تحديث مسار OAuth Callback لاستخدام الأسرار المُسترجَعة بأمان لإتمام عملية المصادقة (Authentication).

### 4. تأمين منطق الأحداث (`src/handlers/eventHandler.js`)
*   **التغيير:** تم تعزيز هذا الملف ليكون مُراقباً للأمان، حيث يذكر المطورين في كل مرة بتحديث أي معالج حدث يتفاعل مع الخارج ليستخدم `SecretManager` أيضاً.

---
## 🚧 التوصيات المستقبلية والتحسين المستمر (Future Roadmap)
1.  **الهدف الأول: التكامل السحابي الحقيقي:** يجب استبدال جميع آليات الـ Fallback في `SecretManager` بالاتصال المباشر بـ **AWS Secrets Manager / HashiCorp Vault**. هذا يرفع مستوى الأمان من "جيد جداً" إلى **المستوى المؤسساتي (Enterprise Grade)**.
2.  **التوثيق:** إنشاء ملف `SECURITY.md` لتوثيق جميع المتغيرات السرية المطلوبة وتفاصيل صلاحياتها ومستويات الوصول إليها.

***هذه الوثيقة هي السجل الهندسي المعتمد لتشغيل وصيانة البوت، ويجب أن تُعاد مراجعتها قبل أي عملية تحديث كبيرة في المستقبل.***